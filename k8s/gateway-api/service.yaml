apiVersion: v1
kind: Service
metadata:
  name: gateway-api
  namespace: fcg
spec:
  selector:
    app: gateway-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: LoadBalancer
