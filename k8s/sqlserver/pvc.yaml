apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sqlserver-pvc
  namespace: fcg
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs.csi.aws.com
  resources:
    requests:
      storage: 10Gi
